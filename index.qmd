---
title: "MODELOS CONTINUOS Y TLC"
subtitle: "SHINYS INTERACTIVOS"
author: 
  - name: Sebastian Bohorquez<br>Juan Zambrano<br>Laura Galindo
    affiliation: "Universidad del Tolima"
    affiliation-url: "https://www.ut.edu.co/"
date: today
lang: es
bibliography: referencias.bib
link-citations: true
nocite: |
  @*
title-block-banner: "#1FB0BF"
filters:
  - shinylive
format:
  html:
    page-layout: full
    fontsize: 14pt
    toc: true
    toc-location: right

    number-sections: true
    code-fold: true
    code-summary: "Ver código"
    theme: dark
    highlight-style: monokai
    html-math-method: katex
    smooth-scroll: true
    link-external-newwindow: true
    code-overflow: scroll
    favicon: favicon.ico
editor: visual

knitr:
  opts_chunk:
    message: false
    warning: false
    fig.align: center
    fig.width: 6
    fig.height: 6
    out.width: "70%"
---

<div style="text-align: right;">
  <img src="logout.png" width="150" alt="logo">
</div>

```{=html}
<style>
body{
  background-color:#ffffff;
  font-size:130%;
  text-align:justify;
}
</style>
```

# Cargar paquetes

```{r}
ipak <- function(pkg){

  options(repos = c(CRAN="https://cloud.r-project.org"))

  new.pkg <- pkg[!(pkg %in% rownames(installed.packages()))]

  if(length(new.pkg)) install.packages(new.pkg, dependencies = TRUE)

  invisible(lapply(pkg, function(p)
    suppressPackageStartupMessages(library(p, character.only = TRUE))
  ))
}

packages <- c("tidyverse","kableExtra","psych","readxl")

ipak(packages)
```

# Distribucion Normal

La distribución normal, también conocida como distribución gaussiana o campana de Gauss, es la distribución de probabilidad continua más importante en la estadística.

Su importancia radica en que muchos fenómenos naturales, sociales y económicos siguen este patrón.

La distribución normal es la distribución de probabilidad continua más usada en estadística y una herramienta fundamental para modelar una inmensa variedad de fenómenos en las ciencias, la ingeniería y la economía. Su popularidad se debe a una propiedad matemática conocida como el Teorema del Límite Central, que establece que la suma o el promedio de un gran número de variables aleatorias independientes (sin importar su distribución original) tiende a seguir una distribución normal.

## Características Clave {.unnumbered}

La distribución normal es un tipo de distribución de probabilidad continua. Esto significa que la variable aleatoria puede tomar cualquier valor dentro de un rango determinado. Se representa por una curva simétrica en forma de campana, donde la mayor parte de los datos se agrupan alrededor del valor central.

**Forma de Campana:** La distribución es simétrica alrededor de su media, creando una curva con forma de campana. El punto más alto de la campana es la media.

**Media, Mediana y Moda Coincidentes:** En una distribución normal perfecta, estos tres valores son iguales y se encuentran en el centro de la curva.

**Asintótica al Eje Horizontal:** La curva se extiende indefinidamente en ambas direcciones, acercándose al eje x pero sin llegar a tocarlo.

**Regla Empírica:** Alrededor del 68% de los datos se encuentran dentro de una desviación estándar de la media (μ±σ), el 95% dentro de dos desviaciones estándar (μ±2σ), y el 99.7% dentro de tres desviaciones estándar (μ±3σ).

-   **Parámetros**

La distribución normal está completamente definida por dos parámetros:

**Media (μ):** Indica la ubicación del centro de la curva y el valor más probable.

**Varianza σ2σ2 o Desviación Estándar (σ):** Mide la dispersión o variabilidad de los datos. Una σ grande resulta en una curva más ancha y aplanada, mientras que una σ pequeña crea una curva más estrecha y alta.

-   **Fórmulas**

La fórmula principal de la distribución normal es la función de densidad de probabilidad (FDP). Esta función no calcula la probabilidad de un punto exacto, sino la densidad de probabilidad en ese punto.

Función de Densidad de Probabilidad (FDP):

$$f(x)=σ2π
​1​e−21​(σx−μ​)2$$

Donde:

**x** es el valor de la variable aleatoria.

**μ** es la media.

**σ** es la desviación estándar.

**e** es el número de Euler (aproximadamente 2.71828).

**π** es la constante pi (aproximadamente 3.14159).

Para calcular probabilidades,se utiliza la distribución normal estándar, que tiene una media de 0 y una desviación estándar de 1. Cualquier variable normal puede estandarizarse usando la siguiente fórmula:

**Formula de Estandarización (puntuación Z):**

$$Z = \frac{x - \mu}{\sigma}$$

La puntuación Z nos dice cuántas desviaciones estándar está un valor dado de la media. Luego, se usan tablas o software estadístico para encontrar las probabilidades correspondientes.

<br>

## Ejercicios

## Ejercicio 6.7 {.unnumbered}

Un investigador cientíﬁco informa que unos ratones vivirán un promedio de 40 meses cuando sus dietas se restringen drásticamente y después se enriquecen con vitaminas y proteínas. Suponiendo que la vidas de tales ratones se distribuyen normalmente con una desviación estándar de 6.3 meses, encuentre la probabilidad de que un ratón dado vivirá:

**a)** más de 32 meses<br>**b)** menos de 28 meses<br>**c)** entre 37 y 49 meses

-   **Parametros**

```{r}
media <- 40 
desviacion_estandar <- 6.3 
```

### Solución a los incisos

::: panel-tabset
#### Inciso A

Cual es probabilidad de que un ratón dado vivirá más de 32 meses.

```{r}
prob_mas_de_32 <- 1 - pnorm(32, mean = media, sd = desviacion_estandar)
cat("a)", round(prob_mas_de_32, 4), "\n")
```

La probabilidad de que un ratón viva más de 32 meses es 0.8979.

**Grafica**

```{r}
media <- 40
desviacion_estandar <- 6.3
rango <- c(media - 3 * desviacion_estandar, media + 3 * desviacion_estandar)
ggplot(data.frame(x = rango), aes(x = x)) +  # Curva de la distribución normal
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion_estandar), 
                n = 500, color = "black") +  # Resaltar el área de interés (X > 32)
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion_estandar),
            fill = "steelblue", xlim = c(32, rango[2]), alpha = 0.5) +  # Etiquetas y títulos
  labs(    title = "Distribucion Normal",    subtitle = "Probabilidad de vivir más de 32 meses",    x = "Vida en meses",    y = "Densidad de Probabilidad"  ) +  theme_bw() +  # Línea para el valor de 32
  geom_vline(xintercept = 32, linetype = "dashed", color = "red") +
  geom_text(aes(x = 32, y = 0.01, label = "32 meses"), hjust = -0.1, vjust = -1)
```

La probabilidad de que un ratón viva más de 32 meses es 89.79%

#### Inciso B

Cual es la probabilidad de que un ratón dado vivirá más de 28 meses.

```{r}
prob_menos_de_28 <- pnorm(28, mean = media, sd = desviacion_estandar)
cat("b)", round(prob_menos_de_28, 4), "\n")
```

La probabilidad de que un ratón viva más de 32 meses es 0.0284.

**Grafica**

```{r}
media <- 40
desviacion_estandar <- 6.3
rango <- c(media - 3 * desviacion_estandar, media + 3 * desviacion_estandar)
ggplot(data.frame(x = rango), aes(x = x)) +  # Curva de la distribución normal
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion_estandar), 
                n = 500, color = "black") +  # Resaltar el área de interés (X < 28)
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion_estandar),
            fill = "orange", xlim = c(rango[1], 28), alpha = 0.5) +  # Etiquetas y títulos
  labs(    title = "Distribucion Normal",    subtitle = "Probabilidad de vivir menos de 28 meses",    x = "Vida en meses",    y = "Densidad de Probabilidad"  ) +  theme_bw() +  # Línea para el valor de 28
  geom_vline(xintercept = 28, linetype = "dashed", color = "red") +
  geom_text(aes(x = 28, y = 0.01, label = "28 meses"), hjust = 1.1, vjust = -1)
```

La probabilidad de que un ratón viva más de 32 meses es 2.84%.

#### Inciso C

Cual es la probabilidad de que un ratón dado vivirá más de 49 meses.

```{r}
prob_c_superior <- pnorm(49, mean = media, sd = desviacion_estandar)
prob_c_inferior <- pnorm(37, mean = media, sd = desviacion_estandar)
prob_c <- prob_c_superior - prob_c_inferior
cat("c)", round(prob_c, 4), "\n")
```

La probabilidad de que un ratón viva más de 32 meses es 0.6065.

**Grafica**

```{r}
media <- 40
desviacion_estandar <- 6.3
rango <- c(media - 3 * desviacion_estandar, media + 3 * desviacion_estandar)
ggplot(data.frame(x = rango), aes(x = x)) +  # Curva de la distribución normal
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion_estandar), 
                n = 500, color = "black") +  # Resaltar el área de interés (37 < X < 49)
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion_estandar),
            fill = "purple", xlim = c(37, 49), alpha = 0.5) +  # Etiquetas y títulos
  labs(    title = "Distribucion Normal",    subtitle = "Probabilidad de vivir entre 37 y 49 meses",    x = "Vida en meses",    y = "Densidad de Probabilidad"  ) +  theme_bw() +  # Líneas para los valores de 37 y 49
  geom_vline(xintercept = 37, linetype = "dashed", color = "red") +
  geom_vline(xintercept = 49, linetype = "dashed", color = "red") +
  geom_text(aes(x = 37, y = 0.01, label = "37"), hjust = 1.1, vjust = -1) +
  geom_text(aes(x = 49, y = 0.01, label = "49"), hjust = -0.1, vjust = -1)
```

La probabilidad de que un ratón viva más de 32 meses es 60.65%.
:::

<br>

## Ejercicio 6.9 {.unnumbered}

Una máquina expendedora de bebidas gaseosas se regula para que sirva un promedio de 200 mililitros por vaso. Si la cantidad de bebida se distribuye normalmente con una desviación estándar igual a 15 mililitros,

**a)** ¿qué fracción de los vasos contendrá más de 224 mililitros?<br>**b)** ¿cuál es la probabilidad de que un vaso contenga entre 191 y 209 mililitros?<br>**c)** ¿cuántos vasos probablemente se derramarán si se utilizan vasos de 230 mililitros para las siguientes 1000 bebidas?

-   **Parametros**

```{r}
media <- 200
desviacion_estandar <- 15
```

### Solución a los incisos

::: panel-tabset
#### Inciso A

¿Qué fracción de los vasos contendrá más de 224 mililitros?

```{r}
prob_a <- 1 - pnorm(224, mean = media, sd = desviacion_estandar)
cat("a)", round(prob_a, 4), "\n")
```

La probabilidad de que una fraccion de vasos contendrá más de 224 mililitros es 0.0548

**Grafica**

```{r}
rango <- c(media - 3 * desviacion_estandar, media + 3 * desviacion_estandar)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion_estandar)) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion_estandar),
            fill = "coral", xlim = c(224, max(rango)), alpha = 0.6) +
  labs(    title = "Probabilidad de que un vaso contenga más de 224 ml",    x = "Cantidad de bebida (ml)",    y = "Densidad de probabilidad"  ) +
  geom_vline(xintercept = 224, linetype = "dashed", color = "red") +  theme_minimal()
```

La probabilidad de que una fraccion de vasos contendrá más de 224 mililitros es 54.8%.

#### Inciso B

¿Cuál es la probabilidad de que un vaso contenga entre 191 y 209 mililitros?

```{r}
media <- 200
desviacion_estandar <- 15
prob_b_superior <- pnorm(209, mean = media, sd = desviacion_estandar)
prob_b_inferior <- pnorm(191, mean = media, sd = desviacion_estandar)
prob_b <- prob_b_superior - prob_b_inferior
cat("b)", round(prob_b, 4), "\n")
```

La probabilidad de que un vaso contenga entre 191 y 209 mililitros es de 0.4515.

**Grafica**

```{r}
rango <- c(media - 3 * desviacion_estandar, media + 3 * desviacion_estandar)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion_estandar)) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion_estandar),
            fill = "darkgreen", xlim = c(191, 209), alpha = 0.6) +
  labs(    title = "Probabilidad de un vaso entre 191 ml y 209 ml",    x = "Cantidad de bebida (ml)",    y = "Densidad de probabilidad"  ) +
  geom_vline(xintercept = 191, linetype = "dashed", color = "red") +
  geom_vline(xintercept = 209, linetype = "dashed", color = "red") +
  theme_minimal()
```

La probabilidad de que un vaso contenga entre 191 y 209 mililitros es de 45.15%.

#### Inciso C

¿Cuántos vasos probablemente se derramarán si se utilizan vasos de 230 mililitros para las siguientes 1000 bebidas?

```{r}
media <- 200
desviacion_estandar <- 15
total_vasos <- 1000
prob_derramarse <- 1 - pnorm(230, mean = media, sd = desviacion_estandar)
vasos_derramados <- prob_derramarse * total_vasos
cat("c)", round(vasos_derramados), "vasos\n")
```

Si se utilizan vasos de 230 mililitros para las siguientes 1000 bebidas probablemente se derramarán 23 vasos.

**Grafica**

```{r}
rango <- c(media - 3 * desviacion_estandar, media + 3 * desviacion_estandar)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion_estandar)) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion_estandar),
            fill = "purple", xlim = c(230, max(rango)), alpha = 0.6) +
  labs(    title = "Probabilidad de que un vaso se derrame",    x = "Cantidad de bebida (ml)",    y = "Densidad de probabilidad"  ) +
  geom_vline(xintercept = 230, linetype = "dashed", color = "red") +
  theme_minimal()
```

Si se utilizan vasos de 230 mililitros para las siguientes 1000 bebidas probablemente se derramarán 23 vasos.
:::

<br>

## Ejercicio 6.11 {.unnumbered}

Un abogado viaja todos los días de su casa en los suburbios a su oﬁ cina en el centro de la ciudad. El tiempo promedio para un viaje sólo de ida es 24 minutos, con una desviación estándar de 3.8 minutos. Suponga que la distribución de los tiempos de viaje está distribuida normalmente.

**a)** ¿Cuál es la probabilidad de que un viaje tome al menos 1/2 hora?<br> **b)** Si la oﬁcina abre a las 9:00 A.M. y él sale diario de su casa a las 8:45 A.M., ¿qué porcentaje de las veces llegará tarde al trabajo?<br>**c)** Si sale de su casa a las 8:35 A.M. y el café se sirve en la oﬁcina de 8:50 A.M. a 9:00 A.M., cuál es la probabilidad de que se pierda el café?<br>**d)** Encuentre la longitud de tiempo por arriba de la cual encontramos e1 15% de los viajes más lentos.<br>**e)** Encuentre la probabilidad de que 2 de los siguientes 3 viajes tomen al menos 1/2 hora.<br>

-   **Parametros**

```{r}
media <- 24
desviacion <- 3.8
```

### Solución a los incisos

:::: panel-tabset
#### Inciso A

¿Cuál es la probabilidad de que un viaje tome al menos 1/2 hora?

```{r}
media <- 24
desviacion <- 3.8
prob_a <- 1 - pnorm(30, mean = media, sd = desviacion)
cat("a)", round(prob_a, 4), "\n")
```

La probabilidad de que un viaje tome al menos 1/2 hora es de 0.0572.

**Grafica**

```{r}
rango <- c(media - 3 * desviacion, media + 3 * desviacion)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion), n = 500) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion), fill = "coral", xlim = c(30, max(rango)), alpha = 0.6) +
  labs(title = "Distribucion Normal", x = "Tiempo de viaje (minutos)", y = "Densidad de Probabilidad") +
  geom_vline(xintercept = 30, linetype = "dashed", color = "red") +
  theme_minimal()
```

La probabilidad de que un viaje tome al menos 1/2 hora es de 5.72%.

#### Inciso B

Si la oﬁcina abre a las 9:00 A.M. y él sale diario de su casa a las 8:45 A.M., ¿qué porcentaje de las veces llegará tarde al trabajo?

```{r}
prob_b <- 1 - pnorm(15, mean = media, sd = desviacion)
porcentaje_b <- prob_b * 100
cat("b)", round(porcentaje_b, 2), "%\n")
```

Si la oﬁcina abre a las 9:00 A.M. y él sale diario de su casa a las 8:45 A.M. 99.11 %xxx llegará tarde al trabajo.

**Grafica**

```{r}
rango <- c(media - 3 * desviacion, media + 3 * desviacion)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion), n = 500) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion),fill = "#3de3c2", xlim = c(15, max(rango)), alpha = 0.6) +
  labs(title = "Distribucion Normal", x = "Tiempo de viaje (minutos)", y = "Densidad de Probabilidad") +
  geom_vline(xintercept = 15, linetype = "dashed", color = "red") +  theme_minimal()
```

Si la oﬁcina abre a las 9:00 A.M. y él sale diario de su casa a las 8:45 A.M. 99.11 % llegará tarde al trabajo.

#### Inciso C

Si sale de su casa a las 8:35 A.M. y el café se sirve en la oﬁcina de 8:50 A.M. a 9:00 A.M., cuál es la probabilidad de que se pierda el café?

::: callout-warning
## Advertencia

Este inciso es un problema de distribución binomial, no normal, ya que se trata de un número discreto de “éxitos” (viajes de más de 30 minutos) en un número fijo de intentos (3 viajes). Por lo tanto, la gráfica no es una curva de densidad normal, sino un gráfico de barras de la distribución binomial.
:::

```{r}
prob_c_antes <- pnorm(15, mean = media, sd = desviacion)
prob_c_despues <- 1 - pnorm(25, mean = media, sd = desviacion)
prob_c <- prob_c_antes + prob_c_despues
cat("c)", round(prob_c, 4), "\n")
```

La probabilidad de que se pierda el café es de 0.4051.

**Grafica**

```{r}
rango <- c(media - 3 * desviacion, media + 3 * desviacion)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion), n = 500) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion), fill = "gray", xlim = c(min(rango), 15), alpha = 0.6) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion), fill = "gray", xlim = c(25, max(rango)), alpha = 0.6) +
  labs(title = "Distribucion Normal", subtitle = "Probabilidad de perder el café", x = "Tiempo de viaje (minutos)", y = "Densidad de Probabilidad") +
  geom_vline(xintercept = 15, linetype = "dashed", color = "red") +
  geom_vline(xintercept = 25, linetype = "dashed", color = "red") +
  theme_minimal()
```

La probabilidad de que se pierda el café es de 40.51%.

#### Inciso D

Encuentre la longitud de tiempo por arriba de la cual encontramos e1 15% de los viajes más lentos.

```{r}
valor_d <- qnorm(0.85, mean = media, sd = desviacion)
cat("d)", round(valor_d, 2), "minutos\n")
```

La longitud de tiempo por arriba de la cual encontramos e1 15% de los viajes más lentos es 27.94 minutos.

**Grafica**

```{r}
media <- 24
desviacion <- 3.8
valor_cuantil <- qnorm(0.85, mean = media, sd = desviacion)
rango <- c(media - 3 * desviacion, media + 3 * desviacion)
library(ggplot2)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion), n = 500) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion), fill = "#11de07", xlim = c(valor_cuantil, max(rango)), alpha = 0.6) +
  labs(    title = "Distribucion Normal", subtitle = "Viajes más lentos (15% superior)",    x = "Tiempo de viaje (minutos)",    y = "Densidad de Probabilidad"  ) +
  geom_vline(xintercept = valor_cuantil, linetype = "dashed", color = "red") +
  geom_text(aes(x = valor_cuantil, y = dnorm(valor_cuantil, mean = media, sd = desviacion), label = paste0(round(valor_cuantil, 2), " min")), vjust = -1, hjust = -0.1) +
  theme_minimal()
```

La longitud de tiempo por arriba de la cual encontramos e1 15% de los viajes más lentos es 27.94 minutos.

#### Inciso E

Encuentre la probabilidad de que 2 de los siguientes 3 viajes tomen al menos 1/2 hora.

```{r}
p_exito <- 1 - pnorm(30, mean = media, sd = desviacion)
n_viajes <- 3
x_exitos <- 2
prob_e <- dbinom(x_exitos, size = n_viajes, prob = p_exito)
cat("e)", round(prob_e, 4), "\n")
```

La probabilidad de que 2 de los siguientes 3 viajes tomen al menos 1/2 hora es 0.0092.

**Grafica**

```{r}
p_exito <- 1 - pnorm(30, mean = 24, sd = 3.8) 
n_viajes <- 3 
exitos_deseados <- 0:n_viajes
probabilidades <- dbinom(exitos_deseados, size = n_viajes, prob = p_exito)
df_binomial <- data.frame( num_exitos = factor(exitos_deseados),  prob = probabilidades)
library(ggplot2)
ggplot(df_binomial, aes(x = num_exitos, y = prob)) +
  geom_bar(stat = "identity", fill = "skyblue") +
  geom_text(aes(label = round(prob, 4)), vjust = -0.5) +
  labs(    title = "Distribución Binomial de viajes que tardan ≥ 30 minutos",   subtitle = "La probabilidad de tener exactamente 2 'éxitos' es del 0.94%", x = "Número de viajes de al menos 30 minutos",   y = "Probabilidad"  ) +
  theme_minimal()
```

La probabilidad de que 2 de los siguientes 3 viajes tomen al menos 1/2 hora es 0.92%.
::::

<br>

## Ejercicio 6.13 {.unnumbered}

La vida promedio de cierto tipo de motor pequeño es de 10 años con una desviación estándar de 2 años. El fabricante reemplaza gratis todos los motores que fallen dentro del periodo de garantía. Si él está dispuesto a reemplazar sólo 3% de los motores que fallan, ¿cuánto tiempo de garantía debería ofrecer? Suponga que la duración de un motor sigue una distribución normal.

-   **Parametros**

```{r}
media <- 10       
desviacion <- 2    
probabilidad_fallo <- 0.03 
```

### Solución

```{r}
tiempo_garantia <- qnorm(probabilidad_fallo, mean = media, sd = desviacion)
cat("El fabricante debería ofrecer una garantía de:", round(tiempo_garantia, 2), "años\n")
```

**Grafica**

```{r}
media <- 10
desviacion <- 2
tiempo_garantia <- qnorm(0.03, mean = media, sd = desviacion)
rango <- c(media - 3 * desviacion, media + 3 * desviacion)
ggplot(data.frame(x = rango), aes(x = x)) +
   stat_function(fun = dnorm, args = list(mean = media, sd = desviacion), n = 500, color = "black") +  
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion),  fill = "purple", xlim = c(min(rango), tiempo_garantia), alpha = 0.6) + 
  labs(    title = "Período de Garantía de los Motores",    subtitle = "El 3% de los motores fallará antes del período de garantía",    x = "Vida útil del motor (años)",    y = "Densidad de Probabilidad"  ) +
  theme_bw() +
  geom_vline(xintercept = tiempo_garantia, linetype = "dashed", color = "red") +
  geom_text(aes(x = tiempo_garantia, y = 0.01, label = paste0(round(tiempo_garantia, 2), " años")), 
            hjust = 1.1, vjust = -1)
```

El fabricante debería ofrecer una garantía de 6.24 años.

------------------------------------------------------------------------

## Ejercicio 6.15 {.unnumbered}

Una compañía paga a sus empleados un salario promedio de \$15.90 por hora con una desviación estándar de \$1.50. Si los salarios se distribuyen aproximadamentede forma normal y se pagan al centavo más 6.15 Una compañía paga a sus empleados un salario promedio de \$15.90 por hora con una desviación estándar de \$1.50. Si los salarios se distribuyen aproximadamente de forma normal y se pagan al centavo más 6.15 Una compañía paga a sus empleados un salario promedio de \$15.90 por hora con una desviación estándar de \$1.50. Si los salarios se distribuyen aproximadamente de forma normal y se pagan al centavo más cercano

**a)** ¿qué porcentaje de los trabajadores reciben salarios entre \$13.75 y \$16.22 inclusive por hora?<br>**b)** ¿el 5% más alto de los salarios por hora de los empleados es mayor a qué cantidad?

-   **Parametros**

```{r}
media <- 15.90
desviacion <- 1.50
limite_inferior <- 13.75 - 0.005
limite_superior <- 16.22 + 0.005
```

### Solución a los incisos

::: panel-tabset
#### Inciso A

¿Qué porcentaje de los trabajadores reciben salarios entre \$13.75 y \$16.22 inclusive por hora?

```{r}
prob_a_superior <- pnorm(limite_superior, mean = media, sd = desviacion)
prob_a_inferior <- pnorm(limite_inferior, mean = media, sd = desviacion)
prob_a <- prob_a_superior - prob_a_inferior
porcentaje_a <- prob_a * 100
cat("a)", round(porcentaje_a, 2), "%\n")
```

**Grafica**

```{r}
rango <- c(media - 3 * desviacion, media + 3 * desviacion)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion), n = 500) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion), fill = "darkgreen", xlim = c(limite_inferior, limite_superior), alpha = 0.6) +
  labs(    title = "Porcentaje de salarios entre $13.75 y $16.22",  x = "Salario por hora ($)", y = "Densidad de probabilidad"  ) +
  geom_vline(xintercept = c(limite_inferior, limite_superior), linetype = "dashed", color = "red") +
  theme_minimal()
```

El porcentaje de trabajadores con salarios entre \$13.75 y \$16.22 es 51.04 %.

#### Inciso B

¿El 5% más alto de los salarios por hora de los empleados es mayor a qué cantidad?

```{r}
media <- 15.90
desviacion <- 1.50
valor_b <- qnorm(0.95, mean = media, sd = desviacion)
cat("b)", round(valor_b, 2), "$\n")
```

El 5% más alto de los salarios es mayor a \$18.37.

**Grafica**

```{r}
rango <- c(media - 3 * desviacion, media + 3 * desviacion)
ggplot(data.frame(x = rango), aes(x = x)) +
  stat_function(fun = dnorm, args = list(mean = media, sd = desviacion), n = 500) +
  geom_area(stat = "function", fun = dnorm, args = list(mean = media, sd = desviacion), fill = "purple", xlim = c(valor_b, max(rango)), alpha = 0.6) +
  labs(    title = "El 5% de salarios más altos",  x = "Salario por hora ($)", y = "Densidad de probabilidad" ) +
  geom_vline(xintercept = valor_b, linetype = "dashed", color = "red") +
  theme_minimal()
```

El 5% más alto de los salarios es mayor a \$18.37.
:::

------------------------------------------------------------------------

# Distribucion Ji Cuadrado

La distribución ji-cuadrado $x^2$ es una distribución de probabilidad continua definida solo para valores positivos, que surge de la suma de los cuadrados de variables normales estándar independientes, dependiendo de un parámetro llamado grados de libertad ($k$). Su media es $k$, su varianza $2k$ y su forma varía según $k$: es muy asimétrica para valores pequeños y se aproxima a una normal al crecer. Su función de densidad se expresa mediante la función gamma, y es fundamental en estadística porque se utiliza en pruebas de hipótesis (bondad de ajuste e independencia en tablas de contingencia), en la construcción de intervalos de confianza para varianzas y en la base de otras distribuciones como la t de Student y la F de Fisher. En términos intuitivos, mide la dispersión total al sumar cuadrados de desviaciones de normales, por lo que resulta clave para analizar varianzas y asociaciones entre variables.

## Parámetros y Características

A diferencia de la distribución normal, la distribución ji cuadrado no está definida por una media y una desviación estándar, sino por un único parámetro:

**Grados de libertad (**$v$ o $df$): Este parámetro determina la forma de la distribución. A medida que los grados de libertad aumentan, la forma de la distribución se vuelve más simétrica y se aproxima a una distribución normal.

**Valores No Negativos:** La variable ji cuadrado $x^2$ solo puede tomar valores positivos, ya que se calcula a partir de la suma de cuadrados.

**Asimétrica a la Derecha:** La distribución es asimétrica con una cola larga hacia la derecha.

**Media y Varianza:** La media de la distribución es igual a los grados de libertad ($\mu=v$), y la varianza es el doble de los grados de libertad $\left(\sigma^2 = 2v\right)$.

**Dominio:** $X \geq 0$ (nunca toma valores negativos).

## Función de densidad de probabilidad

$$f(x; k) = \frac{1}{2^{k/2} \Gamma\left(\frac{k}{2}\right)} x^{\frac{k}{2}-1} e^{-x/2}, \quad x > 0$$

## Usos principales en la estadística

La $x^2$ es fundamental en **estadística inferencial**. Algunos de sus usos:

### Pruebas de hipótesis:

1.  **Chi-cuadrado de bondad de ajuste:** verificar si los datos observados se ajustan a una distribución teórica.
2.  **Chi-cuadrado de independencia:** evaluar si dos variables categóricas son independientes en una tabla de contingencia.

**Intervalos de confianza:** para la **varianza poblacional** de una normal, ya que está relacionada con la suma de cuadrados de normales.

**Modelos más complejos:** forma parte de la familia de distribuciones asociadas a la **F de Fisher** y la **t de Student**, que también dependen de $x^2$

## Familias de Curvas Chi Cuadrado

```{r chi-familia}
x_chi <- seq(0.001, 40, length.out = 500)
df_chi_fam <- bind_rows(lapply(c(4, 5, 10), function(k) {
  data.frame(x = x_chi, y = dchisq(x_chi, k), k = paste0("k = ", k))
}))

ggplot(df_chi_fam, aes(x, y, color = k)) +
  geom_line(linewidth = 1.1) +
  ylim(0, 0.2) +
  labs(title = "Familia de curvas χ²",
       subtitle = "Densidades para distintos grados de libertad",
       x = "x", y = "Densidad", color = "Grados de libertad (k)") +
  theme_minimal()
```

------------------------------------------------------------------------

## Ejercicios

### Ejercicio 8.39

Para una distribución chi cuadrada encuentre:

a.  $\chi^2_{0.025}$ cuando $v = 15$
b.  $\chi^2_{0.01}$ cuando $v = 7$
c.  $\chi^2_{0.05}$ cuando $v = 24$

#### Solucion a los incisos

```{r ej839}
chi_a <- qchisq(1 - 0.025, df = 15)
chi_b <- qchisq(1 - 0.01,  df = 7)
chi_c <- qchisq(1 - 0.05,  df = 24)

cat("[1]", round(chi_a, 5), "\n")
cat("[1]", round(chi_b, 5), "\n")
cat("[1]", round(chi_c, 5), "\n")
```

#### Gráficas

```{r ej839-grafs, fig.show='hold', out.width='33%'}
grafica_chi_doc <- function(df_val, alpha, titulo) {
  x_max <- qchisq(0.999, df_val)
  x  <- seq(0.001, x_max, length.out = 600)
  y  <- dchisq(x, df_val)
  critico <- qchisq(1 - alpha, df_val)
  dat <- data.frame(x = x, y = y)
  shade <- subset(dat, x >= critico)
  ggplot(dat, aes(x, y)) +
    geom_line(color = "#2980B9", linewidth = 1.1) +
    geom_area(data = shade, aes(x, y), fill = "#E74C3C", alpha = 0.5) +
    geom_vline(xintercept = critico, linetype = "dashed", color = "#C0392B") +
    labs(title = titulo,
         subtitle = paste0("df = ", df_val, " , α = ", alpha, " , crítico = ", round(critico, 2)),
         x = "x", y = "Densidad") +
    theme_minimal()
}

grafica_chi_doc(15, 0.025, "Distribución Chi-cuadrado (a)")
grafica_chi_doc(7,  0.01,  "Distribución Chi-cuadrado (b)")
grafica_chi_doc(24, 0.05,  "Distribución Chi-cuadrado (c)")
```

------------------------------------------------------------------------

### Ejercicio 8.41

Para una distribución chi cuadrada encuentre $\chi^2_{\alpha}$ tal que:

a.  $P(X^2 > \chi^2_\alpha) = 0.99$ cuando $v = 4$
b.  $P(X^2 > \chi^2_\alpha) = 0.025$ cuando $v = 19$
c.  $P(37.652 < X^2 < \chi^2_\alpha) = 0.045$ cuando $v = 25$

```{r ej841}
chi_841a <- qchisq(0.01,  df = 4)
chi_841b <- qchisq(0.975, df = 19)
chi_841c <- qchisq(pchisq(37.652, df = 25, lower.tail = FALSE) - 0.045,
                   df = 25, lower.tail = FALSE)

cat("[1]", round(chi_841a, 7), "\n")
cat("[1]", round(chi_841b, 5), "\n")
cat("[1]", round(chi_841c, 5), "\n")
```

#### Gráficas

```{r ej841-grafs, fig.show='hold', out.width='33%'}
# a) región izquierda con espacio inicial
x4a <- seq(0.001, 18, length.out=600)
dat4a <- data.frame(x=x4a, y=dchisq(x4a, 4))

# Al poner x >= 0.4, el sombreado deja libre el inicio de la curva
shade4a <- subset(dat4a, x >= 0.4)

ggplot(dat4a, aes(x, y)) + 
  geom_area(data=shade4a, aes(x, y), fill="#ff9999", alpha=0.8) +
  geom_line(color="blue", linewidth=1.1) + 
  labs(title="Distribución Chi-cuadrado con v = 4",
       subtitle="Caso a con región sombreada", 
       x=expression(X^2), y="Densidad") +
  theme_minimal()

# b)
x4b <- seq(0.001, 45, length.out=600)
dat4b <- data.frame(x=x4b, y=dchisq(x4b,19))
shade4b <- subset(dat4b, x >= chi_841b)
ggplot(dat4b, aes(x,y)) + geom_line(color="#2980B9",linewidth=1.1) +
  geom_area(data=shade4b, aes(x,y), fill="#E74C3C", alpha=0.5) +
  geom_vline(xintercept=chi_841b, linetype="dashed",color="#C0392B") +
  labs(title="Distribución Chi-cuadrado con v = 19",
       subtitle="Caso b con región sombreada", x="X²", y="Densidad") +
  theme_minimal()

# c)
x4c <- seq(0.001, 80, length.out=600)
dat4c <- data.frame(x=x4c, y=dchisq(x4c,25))
shade4c <- subset(dat4c, x >= 37.652 & x <= chi_841c)
ggplot(dat4c, aes(x,y)) + geom_line(color="#2980B9",linewidth=1.1) +
  geom_area(data=shade4c, aes(x,y), fill="#E74C3C", alpha=0.5) +
  geom_vline(xintercept=c(37.652, chi_841c), linetype="dashed",color="#C0392B") +
  labs(title="Distribución Chi-cuadrado con v = 25",
       subtitle="Caso c con región sombreada", x="X²", y="Densidad") +
  theme_minimal()
```

------------------------------------------------------------------------

# .Distribucion T de Student

La **distribución** $t$ de Student es una distribución de probabilidad continua que surge al estimar la media de una población normalmente distribuida cuando el tamaño de la muestra es pequeño y la varianza poblacional es desconocida. Fue introducida en 1908 por William Sealy Gosset bajo el seudónimo de *Student*.

Matemáticamente, si $Z$ es una variable aleatoria con distribución normal estándar $N(0,1)$ y $V$ es una variable con distribución chi-cuadrado con $v$ grados de libertad, independientes entre sí, entonces la variable

$$T = \frac{Z}{\sqrt{V/v}}$$

sigue una distribución $t$ de Student con $v$ grados de libertad.

## Principales características

-   **Forma**: es simétrica respecto al cero, similar a la normal estándar, pero con colas más pesadas.
-   **Parámetro**: depende de los grados de libertad ($v$); a mayor $v$, la distribución se aproxima a $N(0,1)$.
-   **Media**: 0 (para $v > 1$).
-   **Varianza**: $\frac{v}{v-2}$ (para $v > 2$).

### Usos principales

-   Pruebas de hipótesis sobre medias (prueba $t$ de Student)
-   Construcción de intervalos de confianza para las medias
-   Modelar incertidumbre en muestras pequeñas

## Familia de Curvas para T de Student

```{r t-familia}
x_t <- seq(-5, 5, length.out = 500)
df_t_fam <- bind_rows(lapply(c(3, 5, 10), function(k) {
  data.frame(x = x_t, y = dt(x_t, k), df = paste0("df = ", k))
}))

ggplot(df_t_fam, aes(x, y, color = df)) +
  geom_line(linewidth = 1.1) +
  labs(title = "Familia de curvas t-Student",
       subtitle = "Diferentes grados de libertad (df)",
       x = "x", y = "Densidad", color = "") +
  theme_minimal()
```

------------------------------------------------------------------------

## Ejercicios

### Ejercicio 8.47

a)  Encuentre $P(T < 2.365)$ cuando $v = 7$.

<!-- -->

b.  Encuentre $P(T > 1.318)$ cuando $v = 24$.
c.  Encuentre $P(-1.356 < T < 2.179)$ cuando $v = 12$.
d.  Encuentre $P(T > -2.567)$ cuando $v = 17$.

#### Solucion

```{r ej847}
resultados_847 <- data.frame(
  Inciso    = c("a", "b", "c", "d"),
  Resultado = c(
    pt(2.365, df = 7),
    1 - pt(1.318, df = 24),
    pt(2.179, df = 12) - pt(-1.356, df = 12),
    1 - pt(-2.567, df = 17)
  )
)

print(resultados_847, row.names = FALSE)
```

#### Gráficas

```{r ej847-grafs, fig.show='hold', out.width='50%'}
grafica_t_doc <- function(df_val, tipo, v1=NULL, v2=NULL, titulo="") {
  x_t2 <- seq(-5, 5, length.out=600)
  dat_t <- data.frame(x=x_t2, y=dt(x_t2,df_val))
  p <- ggplot(dat_t, aes(x,y)) +
    geom_line(color="#2471A3", linewidth=1.1) +
    labs(title=titulo, x="t", y="Densidad") + theme_minimal()
  if (tipo=="menor" && !is.null(v1)) {
    shade <- subset(dat_t, x <= v1)
    p <- p + geom_area(data=shade, aes(x,y), fill="#E8A0A0", alpha=0.6) +
      geom_vline(xintercept=v1, linetype="dashed", color="gray40")
  } else if (tipo=="mayor" && !is.null(v1)) {
    shade <- subset(dat_t, x >= v1)
    p <- p + geom_area(data=shade, aes(x,y), fill="#E8A0A0", alpha=0.6) +
      geom_vline(xintercept=v1, linetype="dashed", color="gray40")
  } else if (tipo=="entre" && !is.null(v1) && !is.null(v2)) {
    shade <- subset(dat_t, x >= v1 & x <= v2)
    p <- p + geom_area(data=shade, aes(x,y), fill="#AED6F1", alpha=0.7) +
      geom_vline(xintercept=c(v1,v2), linetype="dashed", color="gray40")
  }
  p
}

grafica_t_doc(7,  "menor", v1=2.365,  titulo="a) P(T < 2.365), v = 7\nDistribución t con v = 7")
grafica_t_doc(24, "mayor", v1=1.318,  titulo="b) P(T > 1.318), v = 24\nDistribución t con v = 24")
grafica_t_doc(12, "entre", v1=-1.356, v2=2.179, titulo="c) P(-1.356 < T < 2.179), v = 12\nDistribución t con v = 12")
grafica_t_doc(17, "mayor", v1=-2.567, titulo="d) P(T > -2.567), v = 17\nDistribución t con v = 17")
```

------------------------------------------------------------------------

### Ejercicio 8.49

Dada una muestra aleatoria de tamaño 24 de una distribución normal, encuentre $k$ tal que:

a.  $P(-2.069 < T < k) = 0.965$
b.  $P(k < T < 2.807) = 0.095$
c.  $P(-k < T < k) = 0.90$

#### Solucion

```{r ej849}
k_a <- qt(0.965 + pt(-2.069, df = 23), df = 23)
k_b <- qt(pt(2.807, df = 23) - 0.095,  df = 23)
k_c <- qt(0.95, df = 23)

cat("[1]", round(k_a, 6), "\n")
cat("[1]", round(k_b, 6), "\n")
cat("[1]", round(k_c, 6), "\n")
```

#### Gráficos

```{r ej849-grafs, fig.show='hold', out.width='33%'}
grafica_t_doc(23, "entre", v1=-2.069, v2=k_a,
              titulo="a) P(-2.069 < T < k) = 0.965")
grafica_t_doc(23, "entre", v1=k_b, v2=2.807,
              titulo="b) P(k < T < 2.807) = 0.095")
grafica_t_doc(23, "entre", v1=-k_c, v2=k_c,
              titulo="c) P(-k < T < k) = 0.90")
```

------------------------------------------------------------------------

# .Distribución F de Fisher

La **distribución F**, también conocida como **F de Fisher-Snedecor**, es una distribución de probabilidad **continua** que surge a partir de la razón entre dos variables aleatorias chi-cuadrado normalizadas por sus grados de libertad.

## Definición formal

Si:

-   $X \sim \chi^2_{v_1}$ (una chi-cuadrado con $v_1$ grados de libertad),
-   $Y \sim \chi^2_{v_2}$ (otra chi-cuadrado con $v_2$ grados de libertad), y $X$ y $Y$ son independientes,

entonces la variable:

$$F = \frac{(X/v_1)}{(Y/v_2)}$$

sigue una **distribución F** con $(v_1, v_2)$ grados de libertad.

## Propiedades principales

1.  **Soporte**: $F > 0$, es decir, solo toma valores positivos.
2.  **Asimétrica**: la curva de densidad es sesgada a la derecha.
3.  **Esperanza matemática**: $E(F) = \frac{v_2}{v_2-2}$, para $v_2 > 2$.
4.  **Varianza**: $\text{Var}(F) = \frac{2v_2^2(v_1+v_2-2)}{v_1(v_2-2)^2(v_2-4)}$, para $v_2 > 4$.
5.  Cuando los grados de libertad son grandes, la distribución $F$ tiende a concentrarse cerca de 1.

## Usos más comunes

1.  **Análisis de Varianza (ANOVA):** se utiliza para contrastar si varias medias poblacionales son iguales.
2.  **Pruebas de hipótesis sobre varianzas:** por ejemplo, para comparar si dos poblaciones tienen varianzas iguales.
3.  **Regresión lineal:** la prueba F sirve para evaluar la significancia global del modelo.

## Familia de Curvas F de Fisher

```{r f-familia}
x_f <- seq(0.001, 5, length.out = 500)

params_f <- list(
  list(v1 = 1,  v2 = 5,  label = "v1=1, v2=5"),
  list(v1 = 10, v2 = 20, label = "v1=10, v2=20"),
  list(v1 = 2,  v2 = 10, label = "v1=2, v2=10"),
  list(v1 = 5,  v2 = 10, label = "v1=5, v2=10")
)

df_f_fam <- bind_rows(lapply(params_f, function(p) {
  data.frame(x = x_f, y = df(x_f, p$v1, p$v2), grupo = p$label)
}))

ggplot(df_f_fam, aes(x, y, color = grupo)) +
  geom_line(linewidth = 1.1) +
  ylim(0, 3.5) +
  labs(title = "Familia de curvas de la distribución F",
       x = "x", y = "Densidad", color = "Grados de libertad") +
  theme_minimal()
```

------------------------------------------------------------------------

## Ejercicios

### Ejercicio 8.53

Para una distribución $F$ encuentre:

a.  $f_{0.05}$ con $v_1 = 7$ y $v_2 = 15$
b.  $f_{0.05}$ con $v_1 = 15$ y $v_2 = 7$
c.  $f_{0.01}$ con $v_1 = 24$ y $v_2 = 19$
d.  $f_{0.95}$ con $v_1 = 19$ y $v_2 = 24$
e.  $f_{0.99}$ con $v_1 = 28$ y $v_2 = 12$

```{r ej853}
resultados_853 <- data.frame(
  Inciso  = c("1","2","3","4","5"),
  Detalle = c("a","b","c","d","e"),
  Valor_F = c(
    qf(0.95,  7,  15),
    qf(0.95,  15, 7),
    qf(0.99,  24, 19),
    qf(0.05,  19, 24),
    qf(0.01,  28, 12)
  )
)
print(resultados_853)
```

#### Graficas

```{r ej853-grafs, fig.show='hold', out.width='33%'}
grafica_f_doc <- function(v1, v2, alpha, cola="derecho", titulo="") {
  x_max <- qf(0.999, v1, v2)
  xf <- seq(0.001, x_max, length.out=600)
  yf <- df(xf, v1, v2)
  dat_f <- data.frame(x=xf, y=yf)
  critico <- if (cola=="derecho") qf(1-alpha,v1,v2) else qf(alpha,v1,v2)
  shade_f <- if (cola=="derecho") subset(dat_f,x>=critico) else subset(dat_f,x<=critico)
  ggplot(dat_f, aes(x,y)) +
    geom_line(color="#8E44AD", linewidth=1.1) +
    geom_area(data=shade_f, aes(x,y), fill="#D2B4DE", alpha=0.6) +
    geom_vline(xintercept=critico, linetype="dashed", color="#6C3483") +
    labs(title=titulo,
         subtitle=paste0("v1=",v1,", v2=",v2,", α=",alpha," | F=",round(critico,4)),
         x="x", y="Densidad") + theme_minimal()
}

grafica_f_doc(7,15,0.05,"derecho","f0.05, v1=7, v2=15")
grafica_f_doc(15,7,0.05,"derecho","f0.05, v1=15, v2=7")
grafica_f_doc(24,19,0.01,"derecho","f0.01, v1=24, v2=19")
grafica_f_doc(19,24,0.05,"izquierdo","f0.95, v1=19, v2=24")
grafica_f_doc(28,12,0.01,"izquierdo","f0.99, v1=28, v2=12")
```

# SIMULACIÓN DISTRIBUCIONES

```{shinylive-r}
#| standalone: true
#| viewerHeight: 550

library(shiny)
library(ggplot2)

ui <- fluidPage(
  titlePanel("Distribuciones Continuas Fundamentales"),
  
  sidebarLayout(
    sidebarPanel(
      selectInput("dist_tipo", "Selecciona la Distribución:",
                  choices = c("Distribución Normal" = "normal",
                              "Distribución Ji-Cuadrado (χ²)" = "chisq",
                              "Distribución t de Student" = "t_student",
                              "Distribución F de Fisher" = "fisher")),
      
      hr(),
      h4("Parámetros de la Distribución"),
      
      # Parámetros para la Normal
      conditionalPanel(
        condition = "input.dist_tipo == 'normal'",
        numericInput("norm_mean", "Media (μ):", value = 0),
        numericInput("norm_sd", "Desviación Estándar (σ):", value = 1, min = 0.1, step = 0.1)
      ),
      
      # Parámetros para Ji-Cuadrado
      conditionalPanel(
        condition = "input.dist_tipo == 'chisq'",
        sliderInput("chisq_df", "Grados de libertad (k):", min = 1, max = 100, value = 4, step = 1),
        checkboxInput("show_norm_chisq", "Mostrar aproximación Normal teórica", FALSE)
      ),
      
      # Parámetros para t-Student
      conditionalPanel(
        condition = "input.dist_tipo == 't_student'",
        sliderInput("t_df", "Grados de libertad (v):", min = 1, max = 100, value = 3, step = 1),
        checkboxInput("show_norm_t", "Comparar con Normal Estándar N(0,1)", FALSE)
      ),
      
      # Parámetros para F-Fisher
      conditionalPanel(
        condition = "input.dist_tipo == 'fisher'",
        sliderInput("f_df1", "Grados de libertad del numerador (d1):", min = 1, max = 100, value = 5, step = 1),
        sliderInput("f_df2", "Grados de libertad del denominador (d2):", min = 1, max = 100, value = 10, step = 1)
      )
    ),
    
    mainPanel(
      plotOutput("dist_plot", height = "480px")
    )
  )
)

server <- function(input, output) {
  
  output$dist_plot <- renderPlot({
    
    # Base del gráfico de funciones continuas (estilo limpio y profesional)
    g <- ggplot() + theme_minimal() +
      theme(
        plot.title = element_text(size = 14, face = "bold", hjust = 0.5),
        axis.title = element_text(size = 11),
        legend.position = "bottom"
      )
    
    # ---------------------------------------------
    # 1. DISTRIBUCIÓN NORMAL
    # ---------------------------------------------
    if (input$dist_tipo == "normal") {
      mean_val <- input$norm_mean
      sd_val <- input$norm_sd
      
      # Rango de visualización (4 desviaciones estándar)
      x_min <- mean_val - 4 * sd_val
      x_max <- mean_val + 4 * sd_val
      
      g <- g + 
        stat_function(fun = dnorm, args = list(mean = mean_val, sd = sd_val),
                      linewidth = 1.2, aes(color = "Normal Teórica")) +
        xlim(x_min, x_max) +
        scale_color_manual(values = c("Normal Teórica" = "#2C3E50")) +
        labs(title = paste0("Distribución Normal: N(μ = ", mean_val, ", σ = ", sd_val, ")"),
             x = "Valores (X)", y = "Densidad", color = "")
    }
    
    # ---------------------------------------------
    # 2. DISTRIBUCIÓN JI-CUADRADO
    # ---------------------------------------------
    else if (input$dist_tipo == "chisq") {
      df_val <- input$chisq_df
      
      # Rango dinámico según los grados de libertad
      x_max <- max(20, df_val + 4 * sqrt(2 * df_val))
      
      g <- g + 
        stat_function(fun = dchisq, args = list(df = df_val),
                      linewidth = 1.2, aes(color = "Ji-Cuadrado (χ²)")) +
        xlim(0, x_max) +
        labs(title = paste0("Distribución Ji-Cuadrado con ", df_val, " Grados de Libertad"),
             x = "Valores (X)", y = "Densidad", color = "")
      
      # Añadir aproximación normal si k es grande
      if (input$show_norm_chisq) {
        # E[X] = k, Var(X) = 2k
        g <- g + stat_function(fun = dnorm, args = list(mean = df_val, sd = sqrt(2 * df_val)),
                               linewidth = 1, linetype = "dashed", aes(color = "Aproximación Normal"))
        color_map <- c("Ji-Cuadrado (χ²)" = "#D35400", "Aproximación Normal" = "#7F8C8D")
      } else {
        color_map <- c("Ji-Cuadrado (χ²)" = "#D35400")
      }
      g <- g + scale_color_manual(values = color_map)
    }
    
    # ---------------------------------------------
    # 3. DISTRIBUCIÓN T DE STUDENT
    # ---------------------------------------------
    else if (input$dist_tipo == "t_student") {
      df_val <- input$t_df
      
      g <- g + 
        stat_function(fun = dt, args = list(df = df_val),
                      linewidth = 1.2, aes(color = "t de Student")) +
        xlim(-4, 4) +
        labs(title = paste0("Distribución t de Student con ", df_val, " Grados de Libertad"),
             x = "Valores (t)", y = "Densidad", color = "")
      
      # Comparación con la Normal estándar N(0,1)
      if (input$show_norm_t) {
        g <- g + stat_function(fun = dnorm, args = list(mean = 0, sd = 1),
                               linewidth = 1, linetype = "dashed", aes(color = "Normal Estándar N(0,1)"))
        color_map <- c("t de Student" = "#2980B9", "Normal Estándar N(0,1)" = "#E74C3C")
      } else {
        color_map <- c("t de Student" = "#2980B9")
      }
      g <- g + scale_color_manual(values = color_map)
    }
    
    # ---------------------------------------------
    # 4. DISTRIBUCIÓN F DE FISHER
    # ---------------------------------------------
    else if (input$dist_tipo == "fisher") {
      df1 <- input$f_df1
      df2 <- input$f_df2
      
      # Rango acotado para evitar colas asintóticas infinitas invisibles
      g <- g + 
        stat_function(fun = df, args = list(df1 = df1, df2 = df2),
                      linewidth = 1.2, aes(color = "F de Fisher")) +
        xlim(0, 4) +
        scale_color_manual(values = c("F de Fisher" = "#27AE60")) +
        labs(title = paste0("Distribución F de Fisher (d1 = ", df1, ", d2 = ", df2, ")"),
             x = "Valores (F)", y = "Densidad", color = "")
    }
    
    print(g)
  })
}

shinyApp(ui = ui, server = server)
```

<br>

# SIMULACIÓN TEOREMA DEL LIMITE CENTRAL (TLC)

```{shinylive-r}
#| standalone: true
#| viewerHeight: 500

library(shiny)
library(ggplot2)
library(gridExtra)

ui <- fluidPage(
  titlePanel("Simulación del Teorema Central del Límite (TCL)"),
  
  sidebarLayout(
    sidebarPanel(
      selectInput("dist", "Distribución:",
                  choices = c("Uniforme", "Binomial", "Poisson",
                              "Hipergeométrica", "Exponencial")),
      
      # Parámetros dinámicos
      conditionalPanel(
        condition = "input.dist == 'Uniforme'",
        numericInput("a", "Mínimo (a):", 0),
        numericInput("b", "Máximo (b):", 1)
      ),
      
      conditionalPanel(
        condition = "input.dist == 'Binomial'",
        numericInput("size", "Ensayos (n):", 10, min = 1),
        sliderInput("p", "Probabilidad (p):", value = 0.3, min = 0, max = 1, step = 0.01)
      ),
      
      conditionalPanel(
        condition = "input.dist == 'Poisson'",
        numericInput("lambda_pois", "Lambda:", 4, min = 0.1)
      ),
      
      conditionalPanel(
        condition = "input.dist == 'Hipergeométrica'",
        numericInput("Npop", "Tamaño población (N):", 1000, min = 10),
        numericInput("K", "Éxitos en población (K):", 300, min = 1),
        numericInput("m", "Tamaño de muestra (m):", 20, min = 1)
      ),
      
      conditionalPanel(
        condition = "input.dist == 'Exponencial'",
        numericInput("lambda_exp", "Lambda:", 1.5, min = 0.1)
      ),
      
      hr(),
      
      numericInput("n", "Tamaño de muestra (TCL):", 30, min = 1),
      numericInput("Nsim", "Número de simulaciones:", 10000, min = 1000),
      
      actionButton("simular", "Simular")
    ),
    
    mainPanel(
      plotOutput("plots", height = "450px")
    )
  )
)

server <- function(input, output) {
  
  # CORRECCIÓN AQUÍ: ignoreNULL = FALSE obliga a Shiny a correr la simulación al iniciar la app
  sim_data <- eventReactive(input$simular, {
    
    set.seed(123)
    N <- 100000
    
    if (input$dist == "Uniforme") {
      poblacion <- runif(N, input$a, input$b)
      medias <- replicate(input$Nsim, mean(runif(input$n, input$a, input$b)))
      mu <- (input$a + input$b)/2
      sigma <- (input$b - input$a)/sqrt(12)
    }
    
    else if (input$dist == "Binomial") {
      poblacion <- rbinom(N, input$size, input$p)
      medias <- replicate(input$Nsim, mean(rbinom(input$n, input$size, input$p)))
      mu <- input$size * input$p
      sigma <- sqrt(input$size * input$p * (1 - input$p))
    }
    
    else if (input$dist == "Poisson") {
      poblacion <- rpois(N, input$lambda_pois)
      medias <- replicate(input$Nsim, mean(rpois(input$n, input$lambda_pois)))
      mu <- input$lambda_pois
      sigma <- sqrt(input$lambda_pois)
    }
    
    else if (input$dist == "Hipergeométrica") {
      poblacion <- rhyper(N, m = input$K, n = input$Npop - input$K, k = input$m)
      medias <- replicate(input$Nsim, mean(rhyper(input$n, m = input$K, n = input$Npop - input$K, k = input$m)))
      mu <- input$m * (input$K / input$Npop)
      sigma <- sqrt(input$m * (input$K / input$Npop) * (1 - input$K / input$Npop) * ((input$Npop - input$m)/(input$Npop - 1)))
    }
    
    else if (input$dist == "Exponencial") {
      poblacion <- rexp(N, input$lambda_exp)
      medias <- replicate(input$Nsim, mean(rexp(input$n, input$lambda_exp)))
      mu <- 1 / input$lambda_exp
      sigma <- 1 / input$lambda_exp
    }
    
    list(poblacion = poblacion, medias = medias, mu = mu, sigma = sigma)
  }, ignoreNULL = FALSE)
  
  output$plots <- renderPlot({
    data <- sim_data()
    
    df_pob <- data.frame(x = data$poblacion)
    df_med <- data.frame(x = data$medias)
    
    # Gráfico población
    g1 <- ggplot(df_pob, aes(x = x)) +
      geom_histogram(aes(y = after_stat(density)), bins = 50, fill = "lightgreen", color = "black") +
      stat_function(fun = dnorm, args = list(mean = data$mu, sd = data$sigma), color = "darkgreen", linewidth = 1) +
      labs(title = "Población", x = "Valores", y = "Densidad") +
      theme_minimal()
    
    # Gráfico TCL
    g2 <- ggplot(df_med, aes(x = x)) +
      geom_histogram(aes(y = after_stat(density)), bins = 30, fill = "lightblue", color = "black") +
      stat_function(fun = dnorm, args = list(mean = data$mu, sd = data$sigma/sqrt(input$n)), color = "red", linewidth = 1.2) +
      labs(title = "Distribución muestral de la media", x = "Media muestral", y = "Densidad") +
      theme_minimal()
    
    grid.arrange(g1, g2, ncol = 2)
  })
}

shinyApp(ui = ui, server = server)
```

<br>

# Teorema del límite central

El **Teorema del Límite Central** (TLC) es uno de los conceptos más importantes en estadística. En términos simples, establece que la **distribución de la media de una muestra** se aproxima a una **distribución normal** a medida que el tamaño de la muestra aumenta, sin importar la forma de la distribución original de la población.

### Concepto y Analogía

Imagina que tienes una población con una distribución extraña, por ejemplo, muy sesgada. Si tomas una sola muestra grande de esa población y calculas su media, esa media se comportará de manera predecible. Si repites este proceso muchas veces, las medias de todas esas muestras formarán un histograma que se parecerá cada vez más a una curva de campana (la distribución normal).

Una analogía popular es la de los dados. La distribución de probabilidad de un solo dado es uniforme (cada número tiene la misma probabilidad). Sin embargo, si lanzas muchos dados y sumas sus resultados, la distribución de esas sumas se aproximará a una campana, con los resultados centrales (la media) siendo mucho más probables que los extremos.

### Puntos Clave del Teorema

1.  **Forma de la Distribución de Muestreo:** La distribución de las medias de la muestra se vuelve normal, sin importar si la población original era normal, sesgada, uniforme o de cualquier otra forma.
2.  **Tamaño de la Muestra (n):** La aproximación a la normalidad es más precisa a medida que el tamaño de la muestra ($n$) aumenta. Generalmente, una muestra de $n \geq 30$ es considerada suficiente para que la aproximación sea válida.
3.  **Relación con la Media de la Población:** La media de la distribución de muestreo de las medias ($\mu_{\bar{x}}$) es igual a la media de la población ($\mu$).

$$\mu_{\bar{x}} = \mu$$

4.  **Relación con la Desviación Estándar de la Población:** La desviación estándar de la distribución de muestreo de las medias, llamada **error estándar** ($\sigma_{\bar{x}}$), es igual a la desviación estándar de la población ($\sigma$) dividida por la raíz cuadrada del tamaño de la muestra ($n$). $$\sigma_{\bar{x}} = \frac{\sigma}{\sqrt{n}}$$ Esto significa que, a medida que el tamaño de la muestra aumenta, el error estándar disminuye, y las medias de las muestras se agrupan más estrechamente alrededor de la media de la población.

### Importancia

El Teorema del Límite Central es fundamental porque permite utilizar la **distribución normal** (que es fácil de trabajar) para hacer inferencias sobre la media de la población, incluso cuando no se conoce la distribución de la población original. Esto es la base para muchas pruebas estadísticas, como los intervalos de confianza y las pruebas de hipótesis.

### Simulacion para la distribuicion binomial

### Distribucion binomial original

```{r grafico-binomial, fig.show='hold', out.width='33%'}
k <- 10      # Número de ensayos de la binomial
p <- 0.35    # Probabilidad de éxito

poblacion <- data.frame(
  x = 0:k,
  probabilidad = dbinom(0:k, size = k, prob = p)
)

ggplot(poblacion, aes(x = x, y = probabilidad)) +
  geom_bar(stat = "identity", fill = "#91cc02", alpha = 0.8) +
  labs(
    title = "Distribución Binomial Original",
    x = "Número de Éxitos",
    y = "Probabilidad"
  ) +
  theme_minimal()
```

### Distribucion de las medias muestrales

```{r grafico-medias-muestrales, fig.show='hold', out.width='33%'}
# 1. Simulación: Tomar muchas muestras (ej. 1000) de tamaño n (ej. 30)
set.seed(123) # Para que la simulación sea replicable
n_muestra <- 30
replicaciones <- 1000

# Parámetros de la binomial original (usando tus datos de k=10 y p=0.35)
k_bin <- 10
p_bin <- 0.35

# Generar las medias muestrales
medias_muestrales <- replicate(replicaciones, mean(rbinom(n_muestra, size = k_bin, prob = p_bin)))
datos_medias <- data.frame(media = medias_muestrales)

# Calcular los parámetros teóricos para la curva normal ajustada
media_teorica <- k_bin * p_bin
sd_teorica <- sqrt(k_bin * p_bin * (1 - p_bin)) / sqrt(n_muestra)

# 2. Graficar con ggplot2
ggplot(datos_medias, aes(x = media)) +
  geom_histogram(aes(y = ..density..), binwidth = 0.08, fill = "#91cc02", color = "white", alpha = 0.8) +
  stat_function(fun = dnorm, args = list(mean = media_teorica, sd = sd_teorica), 
                color = "#000080", linetype = "dashed", size = 1.2) +
  labs(
    title = "Distribución de las Medias Muestrales",
    x = "Media de la Muestra",
    y = "Densidad"
  ) +
  theme_minimal()
```

### Simulacion para la distribuicion de Poisson

### Distribucion de Poisson original

```{r grafico-poisson, fig.show='hold', out.width='33%'}
# 1. Definir el parámetro lambda (media de eventos, por ejemplo lambda = 3)
lambda_val <- 3
x_poisson <- 0:15

# Crear el data frame con las probabilidades teóricas de Poisson
poblacion_poisson <- data.frame(
  x = x_poisson,
  probabilidad = dpois(x_poisson, lambda = lambda_val)
)

# 2. Graficar con ggplot2
ggplot(poblacion_poisson, aes(x = x, y = probabilidad)) +
  geom_bar(stat = "identity", fill = "#ff00cc", alpha = 0.9, width = 0.5) +
  labs(
    title = "Distribucion de Poisson origianal",
    x = "Number of Events",
    y = "Probability"
  ) +
  theme_minimal()
```

### Distribucion de las medias muestrales

```{r grafico-medias-poisson, fig.show='hold', out.width='33%'}
# 1. Simulación de medias muestrales de una distribución de Poisson
set.seed(123)
n_muestra <- 30
replicaciones <- 1000
lambda_val <- 3

# Generar las medias muestrales
medias_poisson <- replicate(replicaciones, mean(rpois(n_muestra, lambda = lambda_val)))
datos_medias_poisson <- data.frame(media = medias_poisson)

# Parámetros teóricos de las medias según el TLC
media_teorica <- lambda_val
sd_teorica <- sqrt(lambda_val) / sqrt(n_muestra)

# 2. Graficar con ggplot2
ggplot(datos_medias_poisson, aes(x = media)) +
  geom_histogram(aes(y = ..density..), binwidth = 0.1, fill = "#ff00cc", color = "white", alpha = 0.8) +
  stat_function(fun = dnorm, args = list(mean = media_teorica, sd = sd_teorica), 
                color = "#000080", linetype = "dashed", size = 1.2) +
  labs(
    title = "Distribución de las Medias Muestrales",
    x = "Sample Mean",
    y = "Density"
  ) +
  theme_minimal()
```

### Simulacion para la distribucion Hipergeometrica

### Distribucion Hipergeometrica original

```{r grafico-hipergeometrica, fig.show='hold', out.width='33%'}
# 1. Definir los parámetros de la distribución hipergeométrica
# Ejemplo estándar: M = 50 (total población), N = 20 (éxitos en población), k = 25 (tamaño muestra)
m_exitos <- 20       # Número de elementos con la característica en la población
n_fracasos <- 30     # Número de elementos sin la característica (M - N)
k_muestra <- 25      # Número de elementos extraídos en la muestra

x_hiper <- 0:k_muestra
prob_hiper <- dhyper(x_hiper, m = m_exitos, n = n_fracasos, k = k_muestra)
poblacion_hiper <- data.frame(x = x_hiper, probabilidad = prob_hiper)

# 2. Graficar con ggplot2
ggplot(poblacion_hiper, aes(x = x, y = probabilidad)) +
  geom_bar(stat = "identity", fill = "#828bb2", alpha = 0.9, width = 0.4) +
  labs(
    title = "Distribución Hipergeométrica Original",
    x = "Número de Éxitos en la Muestra",
    y = "Probabilidad"
  ) +
  theme_minimal()
```

### Distribucion de las medias muestrales

```{r grafico-medias-hiper-perfecto, fig.show='hold', out.width='33%'}
# Definir variables de la población (ejemplo base para que calce en 0.4)
N <- 50   # Total de la población
K <- 20   # Éxitos en la población

set.seed(123)
n <- 30      # Tamaño de la muestra
R <- 10000   # Número de repeticiones

medias_muestrales <- replicate(R, {
  exitos_muestra <- rhyper(nn = 1, m = K, n = N - K, k = n)
  exitos_muestra / n
})

df_medias <- data.frame(media = medias_muestrales)

# Graficar exactamente con los parámetros del script guía
ggplot(df_medias, aes(x = media)) +
  geom_histogram(aes(y = after_stat(density)), bins = 30, fill = "#8d8dad", color = "white", alpha = 0.8) +
  stat_function(fun = dnorm, args = list(mean = mean(df_medias$media), sd = sd(df_medias$media)), 
                color = "darkblue", size = 1.2, linetype = "dashed") +
  scale_x_continuous(breaks = seq(0.2, 0.6, by = 0.2), limits = c(0.1, 0.7)) +
  labs(
    title = "Distribución de las Medias Muestrales",
    x = "Media de la Muestra",
    y = "Densidad"
  ) +
  theme_minimal()
```
